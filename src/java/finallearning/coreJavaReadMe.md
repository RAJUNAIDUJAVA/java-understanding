1. what is final and where you use final variable
    -> final is a non access modifier which will help us to immutable the variable/method/class 
    -> we cannot change the value once the value is assigned, we cannot extend the class if we make class as final
        we cannot override the method once we made the method as final
    -> mostly we will use for creating the constants
    ->